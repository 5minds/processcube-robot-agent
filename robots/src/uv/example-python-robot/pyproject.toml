[project]
name = "example-python-robot"
version = "0.1.0"
description = "Example Python Robot using UV package manager"
requires-python = ">=3.11"
dependencies = [
    "robocorp-workitems>=1.0.0",
]

[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"
